XNAT library for R

Original author: Christian Haselgrove

Modified by Kevin Archie <karchie@wustl.edu> for invocation from
inside an XNAT webapp; environment variables JSESSIONID and XNAT_URL
must be set

requires RCurl and XML

example:

    > source('xnat.R')
    > conn <- xnat.connection()  # host,auth specified in env var
    > conn$projects()
    > conn$subjects('test_project')
